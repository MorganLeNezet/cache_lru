[package]
name = "project"
version = "0.1.0"
edition = "2021"

[dependencies]
lru = "0.12.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
criterion = "0.5.1"